<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@+id/mainActivity">
    <activity
        android:id="@+id/mainActivity"
        android:name="dk.dtu.kursusshaker.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/settingsActivity"
        android:name="dk.dtu.kursusshaker.activities.SettingsActivity"
        android:label="settings_activity"
        tools:layout="@layout/settings_activity" />
    <activity
        android:id="@+id/basketActivity"
        android:name="dk.dtu.kursusshaker.activities.BasketActivity"
        android:label="activity_basket"
        tools:layout="@layout/activity_basket" />
    <activity
        android:id="@+id/searchActivity"
        android:name="dk.dtu.kursusshaker.activities.SearchActivity"
        android:label="activity_search"
        tools:layout="@layout/activity_search" />
    <activity
        android:id="@+id/dashboardActivity"
        android:name="dk.dtu.kursusshaker.activities.DashboardActivity"
        android:label="activity_dashboard"
        tools:layout="@layout/activity_dashboard" />
</navigation>